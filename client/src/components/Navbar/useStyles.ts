import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => ({
  logoImg: {
    height: '2rem',
    marginTop: '5px',
    width: '11rem',
  },
  profileName: {
    textAlign: 'right',
    color: 'black',
  },
  logoutName: {
    textAlign: 'center',
    color: 'black',
  },
}));

export default useStyles;
